# Frontend (Vite)
# Optional: defaults to '/api'
# In local dev, Vite proxies '/api' -> http://localhost:8000
#
# If you run frontend in Docker (see docker-compose.yml), it's convenient to point
# the browser directly at the backend:
# VITE_API_BASE_URL=http://localhost:8000/api
